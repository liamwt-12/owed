// For v1, we serve the landing page as static HTML from public/index.html
// This page.tsx redirects the app router root to the static file
// In Phase 5 we'll convert this to a proper React component

export default function HomePage() {
  return null
}
